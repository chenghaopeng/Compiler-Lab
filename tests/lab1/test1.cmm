int mod (int u, int v) {
    return u - u / v * v;
}
int gcd (int a, int b) {
    if (b) return gcd(b, mod(a, b));
    return a;
}
int f (int n) {
    if (n <= 1) return 1;
    return n * f(n - 1);
}
int main () {
    write(gcd(99, 99));
    write(f(10));
    return 0;
}