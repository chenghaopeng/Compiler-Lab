int f1 (int x, int y) { return x + y; }
int f2 (int xx, int yy) { return f1(xx * 2, yy + 1000) + 10086; }
int main () {
    int a[5];
    int b = 2;
    int t = 0;
    a[(b * 2 + 100) / 2 / 2 - 25] = 10086;
    while (t < 5) {
        write(a[t]);
        t = t + 1;
    }
    write(f2(100, a[1]));
    return 0;
}